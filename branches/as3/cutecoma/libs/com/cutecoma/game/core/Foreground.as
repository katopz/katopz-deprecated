package com.cutecoma.game.core
{
	import com.sleepydesign.display.SDSprite;
	
	/**
	 *
	 * Foreground is any assets type that's always on top, but still lower than System layer.
	 * Item in this layer should be dialog
	 *
	 * @author katopz
	 *
	 */
	public class Foreground extends SDSprite
	{
		public function Foreground()
		{
			
		}
	}
}